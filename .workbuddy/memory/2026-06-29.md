# 2026-06-29 工作日志

## 启动开发服务器预览网站

- 项目为 Next.js 16.1.7 (Turbopack) + React 19 + Tailwind CSS 4，名为 Marhoo Studio
- `package.json` 脚本：`dev` = `next dev`，`build` = `next build`，`start` = `next start`
- `next.config.ts` 配置了 `output: "export"`（静态导出），但 `next dev` 仍可用于开发预览
- 依赖已安装在 `node_modules/`，无需额外安装
- 启动时发现端口 3000 已有一个 Next.js dev 实例在运行（进程 19868），新启动因 `.next/dev/lock` 锁文件冲突退出
- 确认现有实例正常工作：HTTP 200，成功渲染首页（Marhoo Studio 产品展示页）
- 预览地址：http://localhost:3000

## 新增产品页面 `/products`

- 在 Ardot 中创建了产品页设计稿（文件 ID: 698557593857905）
- 采用 Nordic-lifestyle 风格：暖奶油底色 #F8F5F0，鼠尾绿 #87A878 强调色，赤陶 #D89575 辅助色
- Space Grotesk 字体（Bold/Medium/Regular），暖色卡片，柔光阴影
- 创建了 `components/pages/products-content.tsx` 组件
- 创建了 `app/products/page.tsx` 路由页
- 修改 `site-header.tsx`：将 "产品" 链接从 `/` 改为 `/products`
- 产品卡片区分 live（TreeHole Adventure - 展示商店链接）和 coming-soon（APP代号：专注 - 展示即将推出标记）
- Next.js build 通过，TypeScript 无错误，所有路由正常

## 产品页设为首页

- 将 `products-content.tsx` 的设计整体搬到 `home-content.tsx`（函数名保持 HomeContent）
- Header "产品" 链接改回 `/`（首页即产品展示）
- `/products` 路由改为 307 redirect 到 `/`，保证旧链接不 404
- 首页现在直接展示两张产品卡片（TreeHole Adventure + APP代号：专注）+ CTA 联系区
- Next.js build 通过，所有路由正常

## 「专注菜菜」产品数据更新

- 抓取了 App Store 真实产品页 (id6756035961)，获取准确信息
- 更新 `data/site.ts` 中 focus 产品条目：
  - 名称：`专注菜菜 - 养成系番茄钟` / `Veggie Focus — Cozy Pomodoro Timer`
  - 标语：`种菜&装扮，可爱治愈的专注计时器`
  - 状态：coming-soon，预计 2026年8月31日 推出
  - 功能亮点：6 项（专注计时/蔬菜养成/家居装扮/标签分类/白噪音/数据统计）
  - 商店链接：已接入 App Store 预购链接
  - 商店信息：免费+IAP、175.4MB、iOS 15.0+、9 种语言、9+ 分级
  - 故事与功能文案全面对标 App Store 介绍
- Next.js build 通过，TypeScript 无错误
